# --- 必须忽略的文件夹 ---
# 虚拟环境（绝对不能传，因为体积巨大且每个人电脑环境不同）
venv/
.venv/
env/

# Python 运行缓存（这是自动生成的，不需要传）
__pycache__/
*.pyc

# --- 保护你的模型和数据 ---
# 忽略所有训练好的模型权重文件（.pth 文件通常很大，GitHub 不适合存大文件）
models/*.pth

# 忽略原始数据文件夹（如果你的 data 文件夹里是几百 MB 的地理高程数据，建议忽略）
# data/

# --- 操作系统与 IDE ---
.DS_Store
.vscode/
.idea/

# --- Streamlit 相关 ---
.streamlit/